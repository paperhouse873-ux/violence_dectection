# Outputs

Runtime artifacts are saved here and ignored by Git:

- `checkpoints/`: model weights.
- `cache/`: extracted numpy features and scalers.
- `results/`: experiment metrics and ablation tables.

For thesis/report evidence, copy small summaries into `reports/` or document them in `docs/evidence/`.
