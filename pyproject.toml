[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "violence-detection-research"
version = "0.1.0"
description = "Research code for violence detection experiments with context gating."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
